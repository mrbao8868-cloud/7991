import React from 'react';

const ExamCreator: React.FC = () => {
    // This component is not currently used.
    // Added content to prevent parsing errors.
    return null;
};

export default ExamCreator;