import React from 'react';

const ExamFinalizer: React.FC = () => {
    // Component is intentionally left empty as its functionality has been
    // merged into ExamWorkspace.tsx to create a unified, step-by-step workflow.
    return null;
};

export default ExamFinalizer;