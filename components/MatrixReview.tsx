import React from 'react';

const MatrixReview: React.FC = () => {
    // This component is no longer used. Its functionality has been integrated
    // directly into the new 'Matrix' tab within the refactored ExamWorkspace component.
    return null;
};

export default MatrixReview;